# دليل التطوير

## ⚠️ تحذير مهم: تجنب التضارب

**لا تشغل البوت محلياً أثناء عمل النسخة المنشورة!**

سيؤدي ذلك إلى:
- تضارب في الردود (نسختان تردان على نفس الرسائل)
- مشاكل في قاعدة البيانات
- تجربة سيئة للمستخدمين

---

## 🔧 التطوير الآمن

### الطريقة 1: استخدم بوت تجريبي منفصل

1. **أنشئ بوت جديد للتطوير:**
   - تحدث مع [@BotFather](https://t.me/BotFather)
   - أرسل `/newbot`
   - اختر اسم مختلف (مثل: YourBot_Dev)
   - احصل على TOKEN الجديد

2. **استخدم TOKEN التطوير محلياً:**
   ```bash
   # في Terminal/CMD
   set TELEGRAM_BOT_TOKEN=your_dev_bot_token
   python bot.py
   ```

3. **البوت الرئيسي يبقى على Heroku/Render**

### الطريقة 2: أوقف البوت المنشور مؤقتاً

إذا كنت تريد اختبار على البوت الرئيسي:

#### على Heroku:
```bash
# إيقاف
heroku ps:scale worker=0

# تشغيل محلي
python bot.py

# إعادة التشغيل على Heroku
heroku ps:scale worker=1
```

#### على Render:
1. Dashboard → البوت → Suspend
2. شغل محلياً: `python bot.py`
3. Dashboard → البوت → Resume

---

## 📝 سير العمل الموصى به

### 1. التطوير:
```bash
# استخدم بوت تجريبي
set TELEGRAM_BOT_TOKEN=dev_token
python bot.py
```

### 2. الاختبار:
- اختبر جميع الميزات على البوت التجريبي
- تأكد من عدم وجود أخطاء

### 3. النشر:
```bash
git add .
git commit -m "وصف التحديث"
git push origin main
```

### 4. التحقق:
- انتظر Deploy على Heroku/Render
- اختبر على البوت الرئيسي
- راقب الـ logs

---

## 🚫 ما يجب تجنبه

❌ **لا تفعل:**
- تشغيل نسختين بنفس TOKEN
- رفع ملف `.env` للـ Git
- تعديل قاعدة البيانات يدوياً

✅ **افعل:**
- استخدم بوت تجريبي للتطوير
- اختبر محلياً قبل النشر
- راجع الـ logs بعد Deploy

---

## 🔍 التحقق من عدم وجود تضارب

### تحقق من العمليات المحلية:
```bash
# Windows
tasklist | findstr python

# Linux/Mac
ps aux | grep python
```

### إيقاف البوت المحلي:
- اضغط `Ctrl + C` في Terminal
- أو أغلق نافذة Terminal

---

## 📊 مراقبة البوت المنشور

### Heroku:
```bash
heroku logs --tail
```

### Render:
- Dashboard → البوت → Logs

---

## ❓ الأسئلة الشائعة

**س: هل يمكن تشغيل نسختين بنفس الوقت؟**
ج: نعم، لكن بـ TOKEN مختلف لكل واحدة.

**س: كيف أعرف إذا كان البوت يعمل محلياً؟**
ج: شوف Terminal - إذا كان مفتوح ويطبع رسائل، يبقى شغال.

**س: ماذا لو نسيت وشغلت نسختين؟**
ج: أوقف واحدة فوراً بـ Ctrl+C أو Suspend.

---

## 🎯 الخلاصة

- 🟢 **للتطوير**: بوت تجريبي محلي
- 🔵 **للإنتاج**: بوت رئيسي على Heroku/Render
- 🔴 **ممنوع**: نسختين بنفس TOKEN!
